name: Build Debian ISO

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
  workflow_dispatch:

jobs:
  build-iso:
    runs-on: ubuntu-latest # ubuntu-22.04 yerine en günceli kullanmak daha iyidir
    timeout-minutes: 120

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install dependencies
        env:
          DEBIAN_FRONTEND: noninteractive
        run: |
          sudo apt-get update
          # Sadece ISO oluşturmak için gerekli olan host paketlerini kuruyoruz.
          # shim-signed ve signed grub paketleri hostta gerekmez, hata verir.
          sudo apt-get install -y \
            live-build \
            debootstrap \
            squashfs-tools \
            xorriso \
            isolinux \
            syslinux \
            syslinux-common \
            grub-pc-bin \
            grub-efi-amd64-bin \
            mtools \
            dosfstools \
            parted \
            curl \
            wget \
            git \
            rsync \
            cpio \
            gzip \
            bzip2 \
            xz-utils \
            zstd \
            apt-utils \
            whois \
            ovmf

      - name: Configure live-build
        run: |
          mkdir -p live-build-config
          cd live-build-config

          lb config noauto \
            --architectures amd64 \
            --mode debian \
            --distribution bookworm \
            --archive-areas "main contrib non-free non-free-firmware" \
            --mirror-bootstrap http://deb.debian.org/debian/ \
            --mirror-chroot http://deb.debian.org/debian/ \
            --mirror-binary http://deb.debian.org/debian/ \
            --mirror-binary-security http://security.debian.org/debian-security/ \
            --mirror-chroot-security http://security.debian.org/debian-security/ \
            --binary-image iso-hybrid \
            --bootloader "syslinux,grub-efi" \
            --bootappend-live "boot=live components quiet splash" \
            --debian-installer none \
            --memtest none \
            --iso-application "MacNix OS" \
            --iso-preparer "MacNix" \
            --iso-publisher "MacNix" \
            --iso-volume "MACNIX_OS" \
            --linux-flavours amd64 \
            --security false \
            --backports false \
            --firmware-binary true \
            --firmware-chroot true \
            --apt-recommends true

      - name: Add package lists
        run: |
          cd live-build-config
          mkdir -p config/package-lists

          cat > config/package-lists/base.list.chroot << 'EOF'
          live-boot
          live-config
          live-config-systemd
          systemd-sysv
          sudo
          bash
          coreutils
          util-linux
          procps
          grep
          sed
          gawk
          findutils
          tar
          gzip
          bzip2
          xz-utils
          curl
          wget
          ca-certificates
          gnupg
          apt-transport-https
          locales
          keyboard-configuration
          console-setup
          tzdata
          EOF

          cat > config/package-lists/desktop.list.chroot << 'EOF'
          task-gnome-desktop
          gnome-core
          gnome-session
          gnome-shell
          gnome-terminal
          gnome-control-center
          gdm3
          network-manager
          network-manager-gnome
          pulseaudio
          firefox-esr
          EOF

          cat > config/package-lists/tools.list.chroot << 'EOF'
          nano
          vim
          htop
          neofetch
          git
          python3
          python3-pip
          EOF

      - name: Add hooks
        run: |
          cd live-build-config
          mkdir -p config/hooks/live

          cat > config/hooks/live/0010-fix-locale.hook.chroot << 'EOF'
          #!/bin/sh
          set -e
          echo "en_US.UTF-8 UTF-8" >> /etc/locale.gen
          locale-gen
          update-locale LANG=en_US.UTF-8
          EOF
          chmod +x config/hooks/live/0010-fix-locale.hook.chroot

          cat > config/hooks/live/0020-enable-gdm.hook.chroot << 'EOF'
          #!/bin/sh
          set -e
          systemctl enable gdm3 || true
          systemctl set-default graphical.target || true
          EOF
          chmod +x config/hooks/live/0020-enable-gdm.hook.chroot

          cat > config/hooks/live/0030-sudo-config.hook.chroot << 'EOF'
          #!/bin/sh
          set -e
          echo "user ALL=(ALL) NOPASSWD: ALL" > /etc/sudoers.d/live-user
          chmod 0440 /etc/sudoers.d/live-user
          EOF
          chmod +x config/hooks/live/0030-sudo-config.hook.chroot

      - name: Add includes
        run: |
          cd live-build-config
          mkdir -p config/includes.chroot/etc/apt
          
          cat > config/includes.chroot/etc/apt/sources.list << 'EOF'
          deb http://deb.debian.org/debian bookworm main contrib non-free non-free-firmware
          deb http://deb.debian.org/debian bookworm-updates main contrib non-free non-free-firmware
          deb http://security.debian.org/debian-security bookworm-security main contrib non-free non-free-firmware
          EOF
          
          mkdir -p config/includes.chroot/etc/macnix
          cat > config/includes.chroot/etc/macnix/version << 'EOF'
          MACNIX_VERSION="1.0"
          EOF

      - name: Build ISO
        run: |
          cd live-build-config
          sudo lb clean --purge 2>/dev/null || true
          rm -rf config/archives
          
          # Build işlemi
          sudo lb build 2>&1 | tee /tmp/lb-build.log
          
          # Hata kontrolü
          if ls *.iso 1> /dev/null 2>&1; then
            echo "✅ ISO build successful!"
          else
            echo "❌ ISO build failed"
            exit 1
          fi
        env:
          DEBIAN_FRONTEND: noninteractive

      - name: Upload ISO artifact
        uses: actions/upload-artifact@v4
        with:
          name: macnix-os-iso
          path: live-build-config/*.iso
          if-no-files-found: error
